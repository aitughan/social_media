import React from "react";
import classes from './Musics.module.css'
import MusicsItem from "./MusicsItem/MusicItem";

const Musics =() =>{
    // debugger;
    // let musicElement = props.state.musicsData.map((m) => 
    //     <tr><td className={classes.tabled}><MusicsItem nameOfSong={m.nameOfSong}/></td>
    //     <td className={classes.tabled}><MusicsItem artist={m.artist}/></td> </tr>)
    return(
        // <div><p className={classes.musics}><table>{musicElement}</table></p></div>
        <div></div>
    )
}

export default Musics       

